# 个人套餐定价与退款

> **这是什么**：运营商面向骑手个人出售的 C 端套餐——卖什么、怎么定价、平台怎么抽 1%、冷静期与中途退款怎么算。  
> **依据**：[PRD.md](./PRD.md) · [骑手电池押金.md](./骑手电池押金.md) · [渠道骑手可换电校验.md](./渠道骑手可换电校验.md) · 文末决策索引  
> **写法**：[业务文档写作规范](./业务文档写作规范.md)

---

## 业务设定

### 一句话

骑手自己付钱买套餐：按天畅换或按次卡；钱进签约运营商，平台从实付抽 1%。开通后 3 个自然日内可走冷静期退（默认要运营商审）；之后可按剩余权益申请中途退。渠道人天骑手**不再**靠「应急自费套餐」换电。

### 和「人天池」的区别

| | 个人套餐 | 人天额度池 |
|--|----------|------------|
| 谁付钱 | 骑手 | 渠道批发 |
| 退款 | 可退给骑手（冷静期 / 中途完结） | 离职天数回池，不退骑手零钱 |
| 平台 1% | 支付成功时按实付 | 确认消耗时按标准人天价 |

### 卖什么套餐（v1）

| 计费 | 名称 | 权益 | 建议零售价（演示） |
|------|------|------|-------------------|
| 按天 | 7 天套餐 | 7 个自然日不限次 | ¥89 |
| 按天 | 30 天畅换 | 30 个自然日不限次 | ¥299 |
| 按天 | 90 天季卡 | 90 个自然日不限次 | ¥799 |
| 按次 | 10 次卡 | 10 次，默认 10 天内有效（可改） | ¥79 / 演示 ¥89 |
| 按次 | 30 次卡 | 30 次，默认 30 天内有效（可改） | ¥199 |

- **按天**：开通当日起算，含首尾共 N 个自然日，末日 23:59 结束。  
- **按次**：次数用完或有效期到期，以先到为准；到期未用完次数作废、不退。  
- **开通方式**（运营商配 SKU 时选）：**购买立即生效**（支付成功当日开通）或 **首次领电生效**（第一次取电当日开通；支付后订单为「待激活」）。  
- **有效期**：自然日，默认等于套餐天数；按次默认 **几次 = 几天**（10 次默认 10 天），可改时长。1 天 / 单次 = 1 个自然日（不再滚动 24h）。  
- **待激活超时**：首次领电模式下，支付后超过有效期仍未领电 → 订单关闭，未使用全额退套餐费。  
- **收入确认**：按天套餐按日摊销（从开通日起）；次卡按次或换电当笔确认。改 SKU 只影响新单。

### 怎么定价

1. **维度**：运营商 × 城市 × 套餐；**不按站点**分别定价。  
2. **通用价**：常规货架价。  
3. **新人价**：仅「直购」首购可享（选站购买或扫柜机码购买）；每用户在该运营商下只用一次；支付成功即消耗资格；中途退款**不恢复**新人资格。  
4. **不适用新人价**：渠道人天、分销链接、平台营销链接、激活码等非直购路径。  
5. **不适用渠道专享价**：链接归因的**续费**与**再次购卡**（曾成交个人购卡）——按运营商**正式零售价**。  
6. **开通方式 / 有效期**：配 SKU 时必选开通方式；有效期默认等于套餐天数，按次默认可改。  
7. **改价 / 下架**：保存后只影响新单；已购订单沿用下单时价格与开通/有效期快照，服务期照常履约。

直购首购取价顺序：有生效新人价 → 用新人价；否则用通用价。  
链接首笔购卡取价：在 24h 归因窗口且满足首笔条件 → 渠道专享价；续费/再次购卡 → 正式零售价。

### 开通冷静期（3 天）

| 项 | 规则 |
|----|------|
| 窗口 | 从**开通服务**起 **3 个自然日**（含开通当日）。立即生效 = 支付日；首次领电 = 领电日 |
| 谁可申请 | 个人套餐、状态「服务中」、**已还电**；待激活未开通、渠道池订单不适用 |
| 待激活退订 | 未领电可全额退套餐费（未使用），不走冷静期扣天 |
| 审核 | **默认必须运营商审核**（不受「自动退款」开关影响） |
| 金额 | 系统按剩余天数给出建议退款，运营商确认时可改实退套餐费与押金 |
| 超时 | 冷静期入口关闭；可改走中途完结等规则 |

天数型建议应退：实付 × 剩余自然日 ÷ 总天数（四舍五入到分）。已还电且实付押金 → 建议全额退押；免押为 0。

### 中途退款

**准入**：服务中、已还电、剩余权益至少 1 天或 1 次；渠道池订单不退给骑手。

| 套餐类型 | 何时可退 | 应退怎么算（白话） |
|----------|----------|-------------------|
| 7 / 30 / 90 天 | 剩余自然日 ≥ 1 | 实付 × 剩余日 ÷ 总天数 |
| 10 / 30 次卡 | 剩余次数 ≥ 1 且未过期 | 实付 × 剩余次数 ÷ 总次数 |
| （若仍上架）1 天畅换 / 单次 · 1 个自然日 | 未换电可全额退；已换电不退 | — |

**自动还是手动**（运营商「退款管理」配置，可与押金退款模式不同）：

- **自动**：条件满足后系统终止订单、原路退应退金额、做清分冲正。  
- **手动**：先待审，确认后再由系统原路退（不是线下手工打款）。

**清分**：从运营商子商户原路退；**平台已收的 1% 不退**；已提现导致余额不够 → 标记须垫付。

### 平台 1%

个人用户（及历史渠道自费路径，若仍存在）在**支付成功**时从运营商收款分账 1%。人天池消耗走 B 端确认消耗计提，见 [天数池.md](./天数池.md)。

### 已取消

- **渠道应急自费套餐**（强制上架 1 天畅换 + 单次、无额度时引导购买）：已取消。渠道骑手无人天时只能还电，见 [渠道骑手可换电校验.md](./渠道骑手可换电校验.md)。  
- **站点覆盖价 / 站点新人套餐**：已取消。

### 后台落在哪

- **运营商 · 定价管理**：通用套餐价、开通方式、有效期（天）、通用新人价（按城市 × SKU）。  
- **运营商 · 退款管理 / 退款设置**：冷静期审核、中途退款自动/手动、与押金退款模式分设。  
- **骑手端 · 购套餐**：展示货架价与应付；新人首购优先新人货架。

### 验收

见 [acceptance-criteria.md](./acceptance-criteria.md) 中退款管理、个人套餐相关条目。

---

## 实现附录（开发用）

> 业务读者可跳过。字段、公式、枚举、决策索引集中于此。

### A. SKU 类型码与计时

| SKU | type | billing | 计时 | 默认有效期 |
|-----|------|---------|------|------------|
| 7 天 | weekly | day | 自然日含首尾 | 7 日，末日 23:59:59 |
| 30 天畅换 | monthly | day | 同上 | 30 |
| 90 天季卡 | quarterly | day | 同上 | 90 |
| 10 次卡 | times | times | 自然日窗口 | **默认 10 日** + swap_limit=10（可改 validity_days） |
| 30 次卡 | times | times | 同上 | **默认 30 日** + swap_limit=30（可改） |
| 1 天畅换 | daily | day | 自然日 | 1 日（开通当日 23:59 结束） |
| 单次 | single | times | 自然日 | 1 日 + swap_limit=1（可改 validity_days） |

`start_mode`：`pay`（购买立即生效）/ `first_pickup`（首次领电生效）。  
立即生效：`valid_from = date(pay_time)`。首次领电：支付后 `status=待激活`；第一次取电当日写入 `valid_from`。  
`valid_to = valid_from + validity_days - 1`，时区默认 Asia/Shanghai。  
待激活超时：`date(pay_time) + validity_days - 1` 仍未领电 → 关单、全额退套餐费。  
权益校验：已开通且 `now ∈ [valid_from, 截止]` 且服务中；次卡另需 `swap_used < swap_limit`。  
按天套餐：`validity_days` 不得短于权益天数。  
冻结：计时暂停，解冻后 `valid_to` 顺延冻结天数。待激活不可冻结。

### B. 定价范围枚举

| priceScope | 含义 | 可删除 |
|------------|------|--------|
| general | 通用套餐 | 是 |
| newcomer_general | 通用新人（仅直购首购） | 是 |
| fallback | 历史渠道兜底（**已废止强制上架**） | — |

定价键：`运营商 × 城市 × priceScope × SKU → retail_price`（无站点维）。  
新人资格：`user_id × operator_id`，仅成功直购个人单消耗；实体 `UserOperatorNewcomer`。

### C. 冷静期 / 中途退款公式

天数型建议 / 应退：`R = round(P × remaining_days / total_days, 2)`  
次卡：`R = round(P × remaining_swaps / total_swaps, 2)`  
冷静期：`cooling_period`，默认人工审，`suggested_R` 可被 `operator_adjusted_R` 覆盖。

### D. 实体摘要

`PkgSku`（含 `start_mode` / `validity_days`）、`SitePkgPrice`（名遗留，实无站点绑定）、`PackageOrder`（`source` / `price_snapshot` / `is_newcomer` / `start_mode` 快照；待激活无 `valid_from`）、`RefundRequest`（类型含冷静期、中途完结、待激活超时退等）。

### E. 决策索引

- 方案 B SKU 体系；定价不绑站点；新人仅直购首购一次  
- 冷静期 3 自然日默认审；自**开通服务**起算（decision-117）  
- 开通方式由 SKU 选择；有效期默认=套餐天数；按次默认几次=几天可改  
- 取消渠道 C 端自费兜底（与渠道校验、天数池一致）  
- 相关：[decision-054](../decisions/decision-054.md) · [decision-117](../decisions/decision-117.md)

### F. 商务确认摘要（历史）

| 议题 | 结果 |
|------|------|
| v1 SKU | 方案 B（骑手常用型） |
| 定价维 | 运营商 × 城市 × SKU |
| 新人 | 通用新人 · 仅直购 · 每 OP 一次 |
| 渠道兜底 SKU | **已取消** |
